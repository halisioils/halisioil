import React from "react";
import WishList from "../_components/WishListPage/WishList";

const WishListPage = async () => {
  return (
    <section className="h-full min-h-[100vh] w-full">
      <WishList />
    </section>
  );
};

export default WishListPage;
